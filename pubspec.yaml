name: xtream_code_client
description: A Dart client for Xtream-compatible IPTV APIs with resilient parsing, diagnostics, and v2 compatibility helpers.
version: 2.0.1
homepage: https://github.com/pauli2406/xtream_code_client
repository: https://github.com/pauli2406/xtream_code_client.git
issue_tracker: https://github.com/pauli2406/xtream_code_client/issues

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  http: ^1.6.0
  json_annotation: ^4.10.0
  xml: ^6.6.1

dev_dependencies:
  build_runner:
  json_serializable: ^6.12.0
  mocktail: ^1.0.4 
  test: ^1.29.0
  very_good_analysis: ^10.2.0

platforms:
  android:
  ios:
  macos:
  web:
  windows:
